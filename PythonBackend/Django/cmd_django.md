django-admin -> for seeing commands used to work with servers

django-admin startproject project_name-> starts the django setup for starting the project 
after this 

python manage.py runserver -> starts the manage.py that is inside the django folder

python manage.py startapp base -> this creates an application that's base level or in base 

{% include 'index.html '%} -> to include one html into another html multiple times 

{% block content %} -> defines replacable section

{% endblock content %} -> closes the block 

{% extends "sser.html" %} -> inherit from base template
